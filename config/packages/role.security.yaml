role:
  security:
    hmac_enabled: true
    hmac_secret: '%env(string:ROLE_PDP_HMAC)%'
    allowed_skew_sec: 300
    anti_replay:
      enabled: true
      ttl_sec: 600
      pdo_dsn: '%env(string:ROLE_AUDIT_DSN)%'
      pdo_user: null
      pdo_pass: null
