# Contributing

- Keep layer-first structure.
- No incomplete endpoint entries — if an endpoint is documented, provide a working implementation and a testable handler.
- Add a smoke script under `tools/rc_*.sh` for every envelope.
