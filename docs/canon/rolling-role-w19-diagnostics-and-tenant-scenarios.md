# Rolling/Role w19 diagnostics and tenant scenarios

- Added business fixtures for multi-tenant isolation, relation override, and deny-by-revocation.
- Extended runner with explain/audit payloads and namespace-aware checks.
- Added CLI diagnostics commands: `app:role:explain` and `app:role:audit`.
