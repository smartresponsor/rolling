# Rolling / Role w13

This wave extracts the winning Admin / Approval slice from `src/Legacy` into canonical Symfony-oriented layers under `src/Service`, `src/ServiceInterface`, `src/Infrastructure`, and `src/InfrastructureInterface`. Legacy classes remain as BC bridges through `legacy_role_w13_aliases.php`.
