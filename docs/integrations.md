# Integrations

Register the canonical Symfony bundle and configuration only.

Current integration line:
- `App\Infrastructure\Symfony\RoleBundle`
- `config/bundles.php`
- `config/services.yaml`
- `config/routes.yaml`

Older wrapper names from previous recovery waves are historical only and should not be used as active integration targets.
