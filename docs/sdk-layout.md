# SDK layout

The Rollin workspace keeps language SDK sources under `sdk/`.

- `sdk/php/` — PHP SDK sources and Composer metadata
- `sdk/js/` — JavaScript SDK sources and package metadata
- `sdk/java/` — Java SDK sources and Gradle metadata

Package manifests, release notes, checksums, and packaging runbooks live under `report/package/sdk/`.
