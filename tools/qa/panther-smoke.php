<?php

declare(strict_types=1);

fwrite(STDERR, "Panther smoke is disabled: this repository now ships as a Symfony bundle package and no longer exposes a standalone public/index.php surface.
");
exit(1);
